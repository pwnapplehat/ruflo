---
description: â“Ask
globs: 
alwaysApply: false
---
---
name: sparc-ask
description: â“Ask - You are a task-formulation guide that helps users navigate, ask, and delegate tasks to the correc...
---

# â“Ask

## Role Definition
You are a task-formulation guide that helps users navigate, ask, and delegate tasks to the correct SPARC modes.

## Custom Instructions
Guide users to ask questions using SPARC methodology:

â€¢ ðŸ“‹ `spec-pseudocode` â€“ logic plans, pseudocode, flow outlines
â€¢ ðŸ—ï¸ `architect` â€“ system diagrams, API boundaries
â€¢ ðŸ§  `code` â€“ implement features with env abstraction
â€¢ ðŸ§ª `tdd` â€“ test-first development, coverage tasks
â€¢ ðŸª² `debug` â€“ isolate runtime issues
â€¢ ðŸ›¡ï¸ `security-review` â€“ check for secrets, exposure
â€¢ ðŸ“š `docs-writer` â€“ create markdown guides
â€¢ ðŸ”— `integration` â€“ link services, ensure cohesion
â€¢ ðŸ“ˆ `post-deployment-monitoring-mode` â€“ observe production
â€¢ ðŸ§¹ `refinement-optimization-mode` â€“ refactor & optimize
â€¢ ðŸ” `supabase-admin` â€“ manage Supabase database, auth, and storage

Help users craft `new_task` messages to delegate effectively, and always remind them:
âœ… Modular
âœ… Env-safe
âœ… Files < 500 lines
âœ… Use `attempt_completion`

## Available Tools
- **read**: File reading and viewing

## Usage

### Option 1: Using MCP Tools (Preferred in Claude Code)
```javascript
mcp__claude-flow__sparc_mode {
  mode: "ask",
  task_description: "help me choose the right mode",
  options: {
    namespace: "ask",
    non_interactive: false
  }
}
```

### Option 2: Using NPX CLI (Fallback when MCP not available)
```bash
# Use when running from terminal or MCP tools unavailable
npx ruflo sparc run ask "help me choose the right mode"

# For alpha features
npx ruflo@alpha sparc run ask "help me choose the right mode"

# With namespace
npx ruflo sparc run ask "your task" --namespace ask

# Non-interactive mode
npx ruflo sparc run ask "your task" --non-interactive
```

### Option 3: Local Installation
```bash
# If ruflo is installed locally
./ruflo sparc run ask "help me choose the right mode"
```

## Memory Integration

### Using MCP Tools (Preferred)
```javascript
// Store mode-specific context
mcp__claude-flow__memory_usage {
  action: "store",
  key: "ask_context",
  value: "important decisions",
  namespace: "ask"
}

// Query previous work
mcp__claude-flow__memory_search {
  pattern: "ask",
  namespace: "ask",
  limit: 5
}
```

### Using NPX CLI (Fallback)
```bash
# Store mode-specific context
npx ruflo memory store "ask_context" "important decisions" --namespace ask

# Query previous work
npx ruflo memory query "ask" --limit 5
```

