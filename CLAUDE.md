# CLAUDE.md (legacy — retained for reference)

> **This file is superseded by [AGENTS.md](AGENTS.md) in the Cursor-native fork
> (pwnapplehat/ruflo).**
>
> Claude Code and Codex integration has been fully removed. `npx ruflo init`
> writes the `.cursor/` tree (`.cursor/mcp.json`, `.cursor/hooks.json`,
> `.cursor/agents/`, `.cursor/skills/`, `.cursor/rules/`) + `AGENTS.md` using
> Cursor's verified hook/MCP/agents contracts. There is no `.claude/` fallback.
>
> **For the active project memory and all workflow instructions, see
> [AGENTS.md](AGENTS.md).** For the full list of changes in this fork, see the
> "Cursor-Native Fork" section in [CHANGELOG.md](CHANGELOG.md).
>
> The historical Claude Code configuration content that previously lived here
> has been removed to avoid presenting Claude Code/Codex as primary hosts.
