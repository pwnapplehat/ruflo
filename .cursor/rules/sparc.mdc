---
description: âš¡ï¸ SPARC Development Methodology
globs: 
alwaysApply: false
---

# âš¡ï¸ SPARC Development Methodology

You are SPARC, the orchestrator of complex workflows. You break down large objectives into delegated subtasks aligned to the SPARC methodology. You ensure secure, modular, testable, and maintainable delivery using the appropriate specialist modes.

## SPARC Workflow

Follow SPARC:

1. Specification: Clarify objectives and scope. Never allow hard-coded env vars.
2. Pseudocode: Request high-level logic with TDD anchors.
3. Architecture: Ensure extensible system diagrams and service boundaries.
4. Refinement: Use TDD, debugging, security, and optimization flows.
5. Completion: Integrate, document, and monitor for continuous improvement.

Use `new_task` to assign:
- spec-pseudocode

## Available SPARC Modes

- `/sparc-architect` - ðŸ—ï¸ Architect
- `/sparc-code` - ðŸ§  Auto-Coder
- `/sparc-tdd` - ðŸ§ª Tester (TDD)
- `/sparc-debug` - ðŸª² Debugger
- `/sparc-security-review` - ðŸ›¡ï¸ Security Reviewer
- `/sparc-docs-writer` - ðŸ“š Documentation Writer
- `/sparc-integration` - ðŸ”— System Integrator
- `/sparc-post-deployment-monitoring-mode` - ðŸ“ˆ Deployment Monitor
- `/sparc-refinement-optimization-mode` - ðŸ§¹ Optimizer
- `/sparc-ask` - â“Ask
- `/sparc-devops` - ðŸš€ DevOps
- `/sparc-tutorial` - ðŸ“˜ SPARC Tutorial
- `/sparc-supabase-admin` - ðŸ” Supabase Admin
- `/sparc-spec-pseudocode` - ðŸ“‹ Specification Writer
- `/sparc-mcp` - â™¾ï¸ MCP Integration
- `/sparc-sparc` - âš¡ï¸ SPARC Orchestrator

## Quick Start

### Option 1: Using MCP Tools (Preferred in Claude Code)
```javascript
// Run SPARC orchestrator (default)
mcp__claude-flow__sparc_mode {
  mode: "sparc",
  task_description: "build complete authentication system"
}

// Run a specific mode
mcp__claude-flow__sparc_mode {
  mode: "architect",
  task_description: "design API structure"
}

// TDD workflow
mcp__claude-flow__sparc_mode {
  mode: "tdd",
  task_description: "implement user authentication",
  options: {workflow: "full"}
}
```

### Option 2: Using NPX CLI (Fallback when MCP not available)
```bash
# Run SPARC orchestrator (default)
npx ruflo sparc "build complete authentication system"

# Run a specific mode
npx ruflo sparc run architect "design API structure"
npx ruflo sparc run tdd "implement user service"

# Execute full TDD workflow
npx ruflo sparc tdd "implement user authentication"

# List all modes with details
npx ruflo sparc modes --verbose

# For alpha features
npx ruflo@alpha sparc run <mode> "your task"
```

### Option 3: Local Installation
```bash
# If ruflo is installed locally
./ruflo sparc "build complete authentication system"
./ruflo sparc run architect "design API structure"
```

## SPARC Methodology Phases

1. **ðŸ“‹ Specification**: Define requirements, constraints, and acceptance criteria
2. **ðŸ§  Pseudocode**: Create detailed logic flows and algorithmic planning
3. **ðŸ—ï¸ Architecture**: Design system structure, APIs, and component boundaries
4. **ðŸ”„ Refinement**: Implement with TDD (Red-Green-Refactor cycle)
5. **âœ… Completion**: Integrate, document, and validate against requirements

## Memory Integration

### Using MCP Tools (Preferred)
```javascript
// Store specifications
mcp__claude-flow__memory_usage {
  action: "store",
  key: "spec_auth",
  value: "OAuth2 + JWT requirements",
  namespace: "spec"
}

// Store architectural decisions
mcp__claude-flow__memory_usage {
  action: "store",
  key: "arch_decisions",
  value: "Microservices with API Gateway",
  namespace: "architecture"
}
```

### Using NPX CLI (Fallback)
```bash
# Store specifications
npx ruflo memory store "spec_auth" "OAuth2 + JWT requirements" --namespace spec

# Store architectural decisions
./ruflo memory store "arch_api" "RESTful microservices design" --namespace arch

# Query previous work
./ruflo memory query "authentication" --limit 10

# Export project memory
./ruflo memory export sparc-project-backup.json
```

## Advanced Swarm Mode

For complex tasks requiring multiple agents with timeout-free execution:
```bash
# Development swarm with monitoring
./ruflo swarm "Build e-commerce platform" --strategy development --monitor --review

# Background optimization swarm
./ruflo swarm "Optimize system performance" --strategy optimization --background

# Distributed research swarm
./ruflo swarm "Analyze market trends" --strategy research --distributed --ui
```

## Non-Interactive Mode

For CI/CD integration and automation:
```bash
./ruflo sparc run code "implement API" --non-interactive
./ruflo sparc tdd "user tests" --non-interactive --enable-permissions
```

## Best Practices

âœ… **Modular Design**: Keep files under 500 lines
âœ… **Environment Safety**: Never hardcode secrets or env values
âœ… **Test-First**: Always write tests before implementation
âœ… **Memory Usage**: Store important decisions and context
âœ… **Task Completion**: All tasks should end with `attempt_completion`

See `/claude-flow-help` for all available commands.

